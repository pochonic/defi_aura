[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && npm install"

[deploy]
startCommand = "python fetch_lending_markets.py --with-sdk-enrichment"
restartPolicyType = "NEVER"
